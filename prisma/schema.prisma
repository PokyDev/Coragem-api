generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ── Enums ─────────────────────────────────────────

enum Category {
  EARCUFF
  ANILLO
  DIJE
  CADENA
  TOPOS
  CANDONGAS
  CONJUNTOS
}

enum Color {
  ROJO
  NEGRO
  BLANCO
  ROSADO
  SILVER
}

enum MovementType {
  PURCHASE // Entrada de stock
  SALE     // Salida de stock
}

// ── Modelos ───────────────────────────────────────

model Product {
  id          String    @id @default(cuid())
  name        String
  price       Int
  category    Category
  color       Color
  stock       Int       @default(0)
  ventas      Int       @default(0)
  isVisible   Boolean   @default(true)
  images      Image[]
  movements   ProductMovement[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  @@index([category])
  @@index([color])
  @@index([isVisible])
}

model ProductMovement {
  id          String        @id @default(cuid())
  productId   String
  product     Product       @relation(fields: [productId], references: [id], onDelete: Cascade)
  type        MovementType
  quantity    Int
  stockBefore Int
  stockAfter  Int
  createdAt   DateTime      @default(now())

  @@index([productId])
  @@index([type])
  @@index([createdAt])
}

model Image {
  id        String   @id @default(cuid())
  productId String
  product   Product  @relation(fields: [productId], references: [id], onDelete: Cascade)
  url       String                        // URL de Cloudinary
  publicId  String                        // public_id de Cloudinary (para eliminar)
  order     Int      @default(0)
  width     Int?
  height    Int?
  createdAt DateTime @default(now())

  @@index([productId])
}

model AdminUser {
  id           String   @id @default(cuid())
  email        String   @unique
  createdAt    DateTime @default(now())
}

model AppConfig {
  key       String   @id
  value     String
  updatedAt DateTime @updatedAt
}

// Rollback a schema sin sync-drive